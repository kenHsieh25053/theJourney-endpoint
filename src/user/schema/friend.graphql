type Query {
  friends(userId: ID!): [Friend!]!
}

type Mutation {
  friend(userAid: String!, userBid: String!): UserRelationshipA!
}

type Friend {
  id: ID!
  friends: [String!]!
  friends_count: Int!
  user: String!
}